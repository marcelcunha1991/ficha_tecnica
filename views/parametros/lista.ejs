<%- include ('../partials/header.ejs') %>
<%- include ('../partials/sidebar.ejs') %>

<div class="container"> 
    <hr>
    <h2>Lista de Parâmetros de Máquina</h2> 
    <div>
        <a class="btn btn-success" href="/parametros/new"> Inserir Novos Parâmetros</a>
        <a class="btn btn-success" href="/parametros/lista"> Listagem - Novos Parâmetros</a>
    </div>
    <hr>

    <h5>Selecione uma máquina:</h5>
    <select  name="maquinasListParametros" id="maquinasListParametros" class="form-control">
        <option value="0"> Selecione uma máquina</option>
        <% maquinas.forEach((maquina) => { %>
            <option value="<%= maquina.id %>">
                <%= maquina.descricao %>
            </option>
        <% }) %>
    </select>   
    <br>
    <br>

    <table id="fichaTable" class="table table-bordered"> 
        <thead>
           <tr>
                <!-- <th> Revisão </th> -->
                <th> Id Parâmetros de Máquina </th>
                <th> Máquina </th>
           </tr>
        </thead>
        <tbody id="tableBody">
        </tbody>
    </table>
</div>

<script src=/scripts/jquery.js></script>
<script src=/scripts/FichaScripts.js></script>

<script>
    function confirmarDelecao(event,form){
       event.preventDefault();
       var decision = confirm("Você quer realmente deletar esta Ficha Técnica?");
        if(decision){
            form.submit();
        }
    }
</script>

<%- include ('../partials/footer.ejs') %>   